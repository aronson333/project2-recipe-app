<section class="new-post-section">
  <a href="/dashboard/new">+ Create new post</button>
</section>


{{#if posts.length}}
<section>
  <h2>Dashboard</h2>
  <ul class="post-list">
    {{#each posts as |post|}}
    <li>
      {{> post-info post}}
    </li>
    {{/each}}
  </ul>
</section>
{{/if}}